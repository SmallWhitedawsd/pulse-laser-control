控制模块功能实现：
1. 输入1个周期~N个周期的脉冲串，输出对应的脉冲，只不过高电平之间夹的时间可调（范围1s~50s可调，进度毫秒级如1234ms）,输入输出的之间的延时越低越好
2. 将脉冲的头尾之间的部分设计为1秒到50秒可调，精度是毫秒级，不带小数点，并且可以使用上位机设置该参数（可以留个串口来调节该参数）

输入波形的参数：0~3.3伏的TTL电平，频率为1K到100K赫兹
输出波形的参数：0~3.3伏的TTL电平，输出的波形最好能够同步，也就是说输入的那一瞬间就马上输出，如果做不到这样子的同步，那允许5个周期内的延迟。